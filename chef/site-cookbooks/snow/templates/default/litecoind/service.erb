start on startup
env NAME=litecoind

script
    echo $$ > /var/run/$NAME.pid
    sudo -Eu ubuntu /usr/bin/litecoind -datadir=/ltc >> /var/log/$NAME.log 2>&1
end script

post-stop script
   rm -f /var/run/$NAME.pid
end script
