var config = require('konfu')
, RippleIn = require('../ripplein')
, rippleIn = new RippleIn({
    conn: require('../lib/db')(config.pg_url, config.pg_native),
    account: config.ripple_account,
    secret: config.ripple_secret
})
rippleIn.init()
